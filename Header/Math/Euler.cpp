#include "Euler.h"
#include "Header/Math/Matrix4x4.h"

Matrix4x4 Euler::ToMatrix() const {
	Matrix4x4 output = {};
	/*
	Matrix4x4::EulerAngle angleOrder[3] = {};
	angleOrder[0] = (Matrix4x4::EulerAngle)((uint8_t)order / 2);
	if (order == EulerOrder::XYZ || order == EulerOrder::ZYX) {
	    angleOrder[1] = Matrix4x4::EulerAngle::Yaw;
	}
	*/

	return output;
}